<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %> | JobPortal</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #f7f7f7; margin: 0; padding: 0; }
        
        /* استایل نهایی برای دیده شدن حتمی Navbar */
        .navbar {
            background-color: #ffffff !important;
            border-bottom: 1px solid #e4e5e7 !important;
            padding: 12px 0 !important;
            /* تغییر به fixed برای اطمینان از قرارگیری در بالاترین لایه */
            position: fixed !important;
            top: 0;
            left: 0;
            right: 0;
            width: 100%;
            z-index: 9999 !important; 
            display: flex !important;
        }

        /* ایجاد فاصله برای بدنه سایت تا محتوا زیر هدر نرود */
        .content-wrapper {
            padding-top: 75px; 
        }

        .navbar-brand {
            font-weight: 900;
            font-size: 26px;
            color: #404145 !important;
            letter-spacing: -1px;
            text-decoration: none;
        }
        .navbar-brand span { color: #1dbf73; }
        
        .nav-link {
            color: #62646a !important;
            font-weight: 600;
            font-size: 15px;
            margin: 0 10px;
            text-decoration: none;
        }
        .nav-link:hover { color: #1dbf73 !important; }
        
        .btn-join {
            border: 1px solid #1dbf73 !important;
            color: #1dbf73 !important;
            font-weight: 600;
            padding: 6px 20px;
            border-radius: 4px;
            text-decoration: none !important;
            transition: all 0.3s;
        }
        .btn-join:hover {
            background-color: #1dbf73;
            color: white !important;
        }
        
        .user-avatar {
            width: 35px;
            height: 35px;
            background-color: #1dbf73;
            color: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            text-decoration: none;
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg shadow-sm">
    <div class="container">
        <a class="navbar-brand" href="/">jobportal<span>.</span></a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <form action="/" method="GET" class="ms-lg-3 d-none d-xl-flex border rounded overflow-hidden">
                <input type="text" name="search" class="form-control border-0 px-3" placeholder="Find Services..." style="width: 250px; box-shadow: none;">
                <button class="btn btn-dark border-0 rounded-0" type="submit"><i class="bi bi-search"></i></button>
            </form>

            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item"><a class="nav-link" href="/">Explore</a></li>
                
                <% if (isAuthenticated) { %>
                    <li class="nav-item"><a class="nav-link text-success fw-bold" href="/jobs/create">Post a Job</a></li>
                    <li class="nav-item"><a class="nav-link" href="/jobs/dashboard">Dashboard</a></li>
                    <li class="nav-item dropdown">
                        <a class="nav-link p-0 ms-2" href="#" id="userDrop" data-bs-toggle="dropdown">
                            <div class="user-avatar text-uppercase"><%= user.username.charAt(0) %></div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-3">
                            <li><a class="dropdown-item py-2 small fw-bold" href="/auth/logout">Logout</a></li>
                        </ul>
                    </li>
                <% } else { %>
                    <li class="nav-item"><a class="nav-link" href="/auth/login">Sign In</a></li>
                    <li class="nav-item"><a class="btn-join ms-lg-2" href="/auth/register">Join</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>

<div class="content-wrapper">